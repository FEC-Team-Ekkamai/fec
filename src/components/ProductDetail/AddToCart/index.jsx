import React from 'react';

var AddToCart = (props) => {
  return (
    // props.props.styles.results.map((icon))
    // <IconEntry />
    <div>test3</div>
  )
}

export default AddToCart;

// return (
//   props.books.map((book, i) => {
//     return (
//       <BookEntry book={book} key={i} selectBook={props.selectBook}/>
//     )
//   })
// )